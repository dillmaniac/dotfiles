[user]
	name = Jan Dillmann
	email = jan.dillmann@kit.edu
	signingkey = 2DF82ACCB284D26F
[core]
  	editor = nano
	whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol	
  	excludesfile = ~/.gitignore
	pager = diff-so-fancy | less --tabs=4 -RFX
	autocrlf = input
[push]
	default = matching
[color]
	ui = auto
[gitflow "prefix"]
	feature = feature-
	release = release-
	hotfix = hotfix-
	support = support-
	versiontag = v
[credential]
    helper = cache --timeout=600
